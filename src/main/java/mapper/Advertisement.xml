<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="advertisement">

    <!--获取首页广告-->
    <select id="getIndexAd" resultType="Advertisement">
        select * from tb_advertisement where type = 0
    </select>

    <!--获取首页广告-->
    <select id="getUserInfoAd" resultType="Advertisement">
        select * from tb_advertisement where type = 1
    </select>

</mapper>